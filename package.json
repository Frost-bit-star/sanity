{
  "name": "sanity-vercel-app",
  "private": true,
  "scripts": {
    "dev": "cd studio && sanity dev",
    "build": "cd studio && sanity build",
    "deploy": "cd studio && sanity deploy"
  },
  "dependencies": {
    "sanity": "latest",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "styled-components": "^6.1.15"
  },
  "engines": {
    "node": ">=18"
  }
}
